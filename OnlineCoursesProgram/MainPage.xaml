<Page
    x:Class="OnlineCoursesProgram.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnlineCoursesProgram"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#333">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="15*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" Background="#777">
            <Button VerticalAlignment="Stretch" Width="100" Click="Courses_Click">Courses</Button>
            <Button VerticalAlignment="Stretch" Width="100">Posts</Button>
        </StackPanel>
        <Frame Grid.Row="1" x:Name="mainFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Lightblue"/>
    </Grid>
</Page>
