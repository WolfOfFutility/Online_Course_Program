<Page
    x:Class="OnlineCoursesProgram.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnlineCoursesProgram"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#333">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="8*" />
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="courseListPanel" Grid.Column="0" Orientation="Vertical" BorderBrush="#CCC" BorderThickness="0,0,1,0" Background="#444">
            <TextBlock Text="Your Enrolled Courses: " Margin="5, 20, 0, 20" Foreground="White" FontSize="20"/>
        </StackPanel>
        <Frame x:Name="subFrame1"  Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
    </Grid>
</Page>
