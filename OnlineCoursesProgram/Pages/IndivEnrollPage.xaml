<Page
    x:Class="OnlineCoursesProgram.Pages.IndivEnrollPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OnlineCoursesProgram.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Background="#333" VerticalScrollMode="Enabled" HorizontalScrollMode="Disabled">
        <StackPanel Orientation="Vertical">
            <Button x:Name="backButton" Margin="20" Content="Back" Click="BackHandler"/>
            <TextBlock x:Name="courseNameField" Text="{x:Bind courseName}" Margin="20, 20, 0, 5" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="authorNameField" Text="{x:Bind authorName}" Margin="20, 0, 20, 0" FontSize="12"/>
            <TextBlock x:Name="courseDescField" Text="{x:Bind courseDesc}" Margin="20" FontSize="15"/>
            <Button x:Name="enrollButton" Content="Enroll In Course" Margin="20, 40, 0, 0" Click="EnrollHandler"/>
        </StackPanel>
    </ScrollViewer>
</Page>
